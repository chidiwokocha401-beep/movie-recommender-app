server {
  listen 80;

  root /usr/share/nginx/html;

  # Same-origin API access (frontend builds with VITE_API_URL="").
  location /api/ {
    proxy_pass http://api:8000/;
    proxy_set_header Host $host;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
